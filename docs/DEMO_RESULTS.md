# Demo & Test Results

Generated by running the repo with the **offline provider** (no AWS credentials).
Reproduce with: `make demo && make test`.

Live Bedrock tests (`tests/test_bedrock_integration.py`) are skipped unless
`LLM_ETL_PROVIDER=bedrock` and AWS credentials are present.

## `python -m pytest`

```
.............ss..................                                        [100%]
31 passed, 2 skipped, 1 warning in 0.57s
```

## `python examples/run_demo.py`

```

========================================================================
1) LLM-AS-COMPILER ETL  (rules compiled once, then run deterministically)
========================================================================
RAW input:
    full_name                       email            phone country  amount currency
Alice Johnson  Alice.Johnson@Example.COM    (415) 555-0100      us  120.50      USD
    Bob Smith             BOB@example.com     415.555.0111      us   89.00      USD
Carlos Mendez           carlos@example.mx +52 55 1234 5678      mx 4200.00      MXN
     Dana Lee        dana.lee@example.com       5105550123      us   15.75      USD
   Emil Novak             emil@example.cz                       cz 7400.00      CZK

GENERATED pandas code (one LLM call per rule, cached thereafter):

# rule: Normalize the phone column to E.164 format (default country code 1).
def transform(df):
    df = df.copy()
    def _e164(v, default_cc='1'):
        digits = _re.sub(r'\D', '', str(v))
        if not digits:
            return None
        if len(digits) == 10:
            digits = default_cc + digits
        return '+' + digits
    df['phone'] = df['phone'].map(_e164)
    df['email'] = df['email'].astype(str).str.strip().str.lower()
    _fx = {'USD': 1.0, 'EUR': 1.08, 'GBP': 1.27, 'MXN': 0.058, 'CZK': 0.043}
    def _to_usd(row):
        cur = str(row.get('currency', 'USD')).upper()
        amt = pd.to_numeric(row.get('amount'), errors='coerce')
        rate = _fx.get(cur, 1.0)
        return round(amt * rate, 2) if amt == amt else None
    df['amount_usd'] = df.apply(_to_usd, axis=1)
    return df

# rule: Split full_name into first_name and last_name.
def transform(df):
    df = df.copy()
    def _e164(v, default_cc='1'):
        digits = _re.sub(r'\D', '', str(v))
        if not digits:
            return None
        if len(digits) == 10:
            digits = default_cc + digits
        return '+' + digits
    df['phone'] = df['phone'].map(_e164)
    parts = df['full_name'].fillna('').str.strip().str.split(n=1, expand=True)
    df['first_name'] = parts[0]
    df['last_name'] = parts[1] if parts.shape[1] > 1 else ''
    df['last_name'] = df['last_name'].fillna('')
    _fx = {'USD': 1.0, 'EUR': 1.08, 'GBP': 1.27, 'MXN': 0.058, 'CZK': 0.043}
    def _to_usd(row):
        cur = str(row.get('currency', 'USD')).upper()
        amt = pd.to_numeric(row.get('amount'), errors='coerce')
        rate = _fx.get(cur, 1.0)
        return round(amt * rate, 2) if amt == amt else None
    df['amount_usd'] = df.apply(_to_usd, axis=1)
    return df

# rule: Normalize email to lowercase and trim whitespace.
def transform(df):
    df = df.copy()
    def _e164(v, default_cc='1'):
        digits = _re.sub(r'\D', '', str(v))
        if not digits:
            return None
        if len(digits) == 10:
            digits = default_cc + digits
        return '+' + digits
    df['phone'] = df['phone'].map(_e164)
    df['email'] = df['email'].astype(str).str.strip().str.lower()
    _fx = {'USD': 1.0, 'EUR': 1.08, 'GBP': 1.27, 'MXN': 0.058, 'CZK': 0.043}
    def _to_usd(row):
        cur = str(row.get('currency', 'USD')).upper()
        amt = pd.to_numeric(row.get('amount'), errors='coerce')
        rate = _fx.get(cur, 1.0)
        return round(amt * rate, 2) if amt == amt else None
    df['amount_usd'] = df.apply(_to_usd, axis=1)
    return df

# rule: Uppercase the country column.
def transform(df):
    df = df.copy()
    def _e164(v, default_cc='1'):
        digits = _re.sub(r'\D', '', str(v))
        if not digits:
            return None
        if len(digits) == 10:
            digits = default_cc + digits
        return '+' + digits
    df['phone'] = df['phone'].map(_e164)
    df['country'] = df['country'].astype(str).str.upper()
    _fx = {'USD': 1.0, 'EUR': 1.08, 'GBP': 1.27, 'MXN': 0.058, 'CZK': 0.043}
    def _to_usd(row):
        cur = str(row.get('currency', 'USD')).upper()
        amt = pd.to_numeric(row.get('amount'), errors='coerce')
        rate = _fx.get(cur, 1.0)
        return round(amt * rate, 2) if amt == amt else None
    df['amount_usd'] = df.apply(_to_usd, axis=1)
    return df

# rule: Convert currency amounts to USD.
def transform(df):
    df = df.copy()
    def _e164(v, default_cc='1'):
        digits = _re.sub(r'\D', '', str(v))
        if not digits:
            return None
        if len(digits) == 10:
            digits = default_cc + digits
        return '+' + digits
    df['phone'] = df['phone'].map(_e164)
    _fx = {'USD': 1.0, 'EUR': 1.08, 'GBP': 1.27, 'MXN': 0.058, 'CZK': 0.043}
    def _to_usd(row):
        cur = str(row.get('currency', 'USD')).upper()
        amt = pd.to_numeric(row.get('amount'), errors='coerce')
        rate = _fx.get(cur, 1.0)
        return round(amt * rate, 2) if amt == amt else None
    df['amount_usd'] = df.apply(_to_usd, axis=1)
    return df

TRANSFORMED output:
    full_name                     email         phone country  amount currency  amount_usd first_name last_name
Alice Johnson alice.johnson@example.com  +14155550100      US  120.50      USD      120.50      Alice   Johnson
    Bob Smith           bob@example.com  +14155550111      US   89.00      USD       89.00        Bob     Smith
Carlos Mendez         carlos@example.mx +525512345678      MX 4200.00      MXN      243.60     Carlos    Mendez
     Dana Lee      dana.lee@example.com  +15105550123      US   15.75      USD       15.75       Dana       Lee
   Emil Novak           emil@example.cz           NaN      CZ 7400.00      CZK      318.20       Emil     Novak

Loaded -> /home/user/LLM-ETL-Data-pipeline/data/customers_clean.csv

========================================================================
2) DETERMINISTIC JOIN ENRICHMENT  (multi-table -> code, not per-row LLM)
========================================================================
Lookup / dimension table:
country        region
     US North America
     MX North America
     CZ        Europe

Enriched (region joined on country):
first_name country        region  amount_usd
     Alice      US North America      120.50
       Bob      US North America       89.00
    Carlos      MX North America      243.60
      Dana      US North America       15.75
      Emil      CZ        Europe      318.20

========================================================================
3a) RUNTIME EXTRACTION - SYNCHRONOUS  (unstructured text -> structured JSON)
========================================================================

TEXT : Payment of $120.50 received from Alice Johnson on 2026-01-04, thanks!
JSON : {'amount': 120.5, 'currency': 'USD', 'customer': 'Alice Johnson', 'date': '2026-01-04', 'sentiment': 'positive'}

TEXT : Refund requested by Bob Smith - item arrived broken and late. $89 USD.
JSON : {'amount': 89.0, 'currency': 'USD', 'customer': 'Bob Smith', 'date': None, 'sentiment': 'negative'}

TEXT : Carlos Mendez paid 240.00 on 2026-02-11. Great service.
JSON : {'amount': 240.0, 'currency': None, 'customer': None, 'date': '2026-02-11', 'sentiment': 'positive'}

========================================================================
3b) RUNTIME EXTRACTION - BEDROCK BATCH INFERENCE  (async job lifecycle)
========================================================================
submit()  -> job_id = offline-e39e1d6a627f
status()  -> COMPLETED
results() ->
  {'amount': 120.5, 'currency': 'USD', 'customer': 'Alice Johnson', 'date': '2026-01-04', 'sentiment': 'positive'}
  {'amount': 89.0, 'currency': 'USD', 'customer': 'Bob Smith', 'date': None, 'sentiment': 'negative'}
  {'amount': 240.0, 'currency': None, 'customer': None, 'date': '2026-02-11', 'sentiment': 'positive'}

run_batch() one-shot returned 3 records (same as above).

========================================================================
4) NATURAL-LANGUAGE QUERY API  (NL question -> query plan -> pandas)
========================================================================
GET /health -> {'status': 'ok', 'rows': 5, 'columns': ['full_name', 'email', 'phone', 'country', 'amount', 'currency', 'amount_usd', 'first_name', 'last_name', 'region']}

Q: How many customers are there?
A: {'answer': 5, 'plan': {'op': 'count'}}

Q: What is the average amount in USD?
A: {'answer': 157.41, 'plan': {'op': 'avg', 'col': 'amount_usd'}}

Q: What is the total amount_usd?
A: {'answer': 787.05, 'plan': {'op': 'sum', 'col': 'amount_usd'}}

Q: Which customer has the maximum amount_usd?
A: {'answer': {'full_name': 'Emil Novak', 'email': 'emil@example.cz', 'phone': None, 'country': 'CZ', 'amount': 7400.0, 'currency': 'CZK', 'amount_usd': 318.2, 'first_name': 'Emil', 'last_name': 'Novak', 'region': 'Europe'}, 'plan': {'op': 'extreme', 'which': 'max', 'col': 'amount_usd'}}

Q: Show me the top 3 by amount_usd
A: {'answer': [{'full_name': 'Emil Novak', 'email': 'emil@example.cz', 'phone': None, 'country': 'CZ', 'amount': 7400.0, 'currency': 'CZK', 'amount_usd': 318.2, 'first_name': 'Emil', 'last_name': 'Novak', 'region': 'Europe'}, {'full_name': 'Carlos Mendez', 'email': 'carlos@example.mx', 'phone': '+525512345678', 'country': 'MX', 'amount': 4200.0, 'currency': 'MXN', 'amount_usd': 243.6, 'first_name': 'Carlos', 'last_name': 'Mendez', 'region': 'North America'}, {'full_name': 'Alice Johnson', 'email': 'alice.johnson@example.com', 'phone': '+14155550100', 'country': 'US', 'amount': 120.5, 'currency': 'USD', 'amount_usd': 120.5, 'first_name': 'Alice', 'last_name': 'Johnson', 'region': 'North America'}], 'plan': {'op': 'top', 'n': 3, 'by': 'amount_usd'}}

Q: List customers in US
A: {'answer': [{'full_name': 'Alice Johnson', 'email': 'alice.johnson@example.com', 'phone': '+14155550100', 'country': 'US', 'amount': 120.5, 'currency': 'USD', 'amount_usd': 120.5, 'first_name': 'Alice', 'last_name': 'Johnson', 'region': 'North America'}, {'full_name': 'Bob Smith', 'email': 'bob@example.com', 'phone': '+14155550111', 'country': 'US', 'amount': 89.0, 'currency': 'USD', 'amount_usd': 89.0, 'first_name': 'Bob', 'last_name': 'Smith', 'region': 'North America'}, {'full_name': 'Dana Lee', 'email': 'dana.lee@example.com', 'phone': '+15105550123', 'country': 'US', 'amount': 15.75, 'currency': 'USD', 'amount_usd': 15.75, 'first_name': 'Dana', 'last_name': 'Lee', 'region': 'North America'}], 'count': 3, 'plan': {'op': 'filter', 'col': 'country', 'eq': 'US'}}

Q: Group customers by country
A: {'answer': {'US': 3, 'CZ': 1, 'MX': 1}, 'plan': {'op': 'group', 'by': 'country', 'agg': 'count'}}

========================================================================
DEMO COMPLETE
========================================================================
```
